import { Counter } from "./components/Counter"
import { Title } from "./components/Title"

export const App = () => {
  return (
    <section className="container">
      <Title/>
      <Counter/>
    </section>
    
  )
}
